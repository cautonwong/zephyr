# 开启模拟器子系统
CONFIG_EMUL=y

# 开启 ADC 子系统及 ADC 模拟器驱动
CONFIG_ADC=y
CONFIG_ADC_EMUL=y

# 开启 UART 子系统（native_sim 默认已开，这里确保开启）
CONFIG_SERIAL=y

# OTA & janpatch 测试
CONFIG_APP_FEATURE_OTA=y
CONFIG_JANPATCH=y
CONFIG_SHELL=y
CONFIG_HEAP_MEM_POOL_SIZE=49152
